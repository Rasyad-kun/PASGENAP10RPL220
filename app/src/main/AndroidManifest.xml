<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.pasgenap10rpl220">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/contact1"
        android:label="Aplikasi PAS"
        android:roundIcon="@android:color/transparent"
        android:supportsRtl="true"
        android:theme="@style/Theme.PASGENAP10RPL220">
        <activity android:name=".Input">

        </activity>
        <activity
            android:name=".DetailFile"
            android:parentActivityName=".MainActivity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>